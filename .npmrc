# next-auth v5 beta peer-declares nodemailer ^7; we use nodemailer 8.x for security fixes (GHSA-c7w3-x93f-qmm8, GHSA-vvjj-xcjg-gr5g).
legacy-peer-deps=true
# Match package.json engines — avoids obscure Prisma 7 / zeptomatch failures on older Node.
engine-strict=true
